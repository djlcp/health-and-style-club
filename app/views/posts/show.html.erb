<section class="post-main">

<!-- <div class="post-category">
<#%= @post.category.name %>
</div> -->

<!-- Hero image -->
	<div class="post-image-main profile-background-container border border-danger d-flex align-items-center">
		<%= image_tag(@post.image_url.to_s, class:"post-background") %>
	</div>


		<h1>
			<%= @post.title %>
		</h1>
<br>
		<h3>
			<%= @post.post_description %>
		</h3>
		<br>

	<div class="post-body-main iframe video">
		<%= sanitize @post.body_text, tags: %w(p ul li ul iframe) %>
	</div>

	<% @post.contents.each do |content| %>

		<p>
	    <%= render "layouts/post_content/#{content.content_type}.html.erb", :@content => content %>
	  </p>
	<% end %>

	<div class="btn-group">
		<%= link_to like_post_path(@post), method: :put, class: "btn btn-default btn-sm" do %>
		<span class="glyphicon glyphicon-chevron-up"></span>
		Upvote
		<%= @post.get_upvotes.size %>
	<% end %>
					</div>

About the author

<%= render 'author' %>

<!--The below generates n error, so commenting out

	<#%= render partial: 'comments/form', locals: { comment: @comment } %>

-->


</section>

<%= link_to 'Edit post', edit_post_path %>
