<div class="content-section">
  <div class="content-wrap">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card">
          <header class="card-header">
            <h2>Edit Collection</h2>
          </header>
          <div class="card-body">

            <%= form_for([:admins, @collection], url: admins_collection_path(@collection), class: 'form') do |f| %>

              <%= render 'form', f: f %>

            <% end %>

          </div>

        </div>

      </div>
    </div>
  </div>
</div>