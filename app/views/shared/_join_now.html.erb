<% if !user_signed_in? %>
  <div class="content-section content-section--dark-grey">
    <div class="content-wrap">
      <h3 class="text-light-pink text-center text-playfair">Ready to become a member?</h3>
      <div class="text-center">
        <%= link_to new_user_registration_path, class: 'btn btn-lg btn-pink mt-3' do %>
          Sign Up Now
        <% end %>
      </div>
    </div>
  </div>
<% end %>